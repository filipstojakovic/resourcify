server:
  port: ${PORT:8083}

logging:
  level:
    org:
      springframework:
        security: DEBUG
      boot:
        autoconfigure: INFO # for dev tools logs

spring:
  devtools:
    livereload:
      port: 38083
  config:
    import: classpath:gateway.yml
