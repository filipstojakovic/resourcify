server:
  port: 8082
  error:
    include-message: always

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000/realms/resourcify-realm

logging:
  level:
    org:
      springframework:
        security: DEBUG

feign:
  name: user-client
  url: "http://localhost:8084"
